<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  package="hc.com.app">

	<application
			android:allowBackup="true"
			android:icon="@mipmap/ic_launcher"
			android:name="com.hc.libs_base.MyApplication"
			android:label="@string/app_name"
			android:supportsRtl="true"
			android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
		<!--闪屏-->
		<activity android:name=".ui.splashactivty.SplashActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<!--登录-->
		<activity android:name=".ui.loginactivity.LoginActivity">

		</activity>
		<!--注册-->
		<activity android:name=".ui.registeractivity.RegisterActivity">

		</activity>
		<!--主窗口-->
		<activity android:name=".MainActivity">

		</activity>


	</application>

</manifest>